# common file for bash, zsh,etc.. all other shell

# C-s で画面が停止するのをやめる. 代わりに履歴検索が使える
stty stop undef

# lsを色付け
export CLICOLOR=1
export LSCOLORS=DxGxcxdxCxegedabagacad

# history on memory
export HISTSIZE=10000

# history on histfile
export SAVEHIST=20000
export HISTFILESIZE=20000

# historyに時刻追加
HISTTIMEFORMAT='%Y%m%d %T ';
export HISTTIMEFORMAT

export EDITOR=/usr/bin/vim
export BUNDLER_EDITOR=vim

# homebrew
export PATH=/usr/local/bin:$PATH

export PATH="$PATH:/usr/local/sbin"

export PATH=/usr/local/share/python:$PATH

[[ -s /usr/local/bin/ghc-pkg ]] && export PATH=~/.cabal/bin:$PATH

# tmuxinator
[[ -s $HOME/.tmuxinator/scripts/tmuxinator ]] && source $HOME/.tmuxinator/scripts/tmuxinator

# nodebrew
if [[ -f $HOME/.nodebrew/nodebrew ]]; then
    export PATH=$HOME/.nodebrew/current/bin:$PATH
fi

# golang
export GOPATH=$HOME/go
export PATH=$PATH:$GOPATH/bin
export PATH=$PATH:/usr/local/opt/go/libexec/bin

#+---------------------------------------------------------+
# grep
#+---------------------------------------------------------+

# grepデフォルトオプションの設定
export GREP_OPTIONS

# バイナリファイルにはマッチさせない
export GREP_OPTIONS="--binary-files=without-match"

# grep対象としてディレクトリを指定したらディレクトリ内を再帰的にgrep
# GREP_OPTIONS="--directories=recurse $GREP_OPTIONS"

GREP_OPTIONS="--exclude=tags $GREP_OPTIONS"

#+---------------------------------------------------------+
# alias
#+---------------------------------------------------------+

alias ll='ls -hl'
alias p="popd"

alias g='git'

alias ctags="`brew --prefix`/bin/ctags"

# man を日本語でも選べるようにする
alias man='env LANG=C man'
alias jman='env LANG=ja_JP.UTF-8 man'

alias be="bundle exec"

# MacVimがあるときは使う
if [ -e /Applications/MacVim.app/Contents/MacOS/Vim ] ; then
  alias vim='env LANG=ja_JP.UTF-8 /Applications/MacVim.app/Contents/MacOS/Vim "$@"'
fi

if [ -e "`brew --prefix`/bin/emacs" ] ; then
  alias e="`brew --prefix`/bin/emacs"
fi

# vim:set filetype=sh:
