#!/bin/sh

stty stop undef  # C-S for history search
stty start undef # C-Q

# cd support
. ~/.zsh/plugins/z/z.sh

if test -d ~/shrc.d/; then
  for shf in ~/shrc.d/*.sh; do
    test -r "$shf" && . "$shf"
  done
  unset shf
fi
